# E1 V2: misallocation aggregate at the point estimate

**Date:** 2026-05-18 (re-verified 2026-05-20).
**Branch:** lca-inversion.
**Plan reference:** Milestone V2 of [the counterfactual plan](file:///C:/git/ckt/.claude/worktrees/lca-inversion/quality_reports/plans/2026-05-18-counterfactual-experiments.md).
**Files:**

- Exporter: [RP7/scripts/_export_e1_inputs.do](file:///C:/git/ckt/.claude/worktrees/lca-inversion/RP7/scripts/_export_e1_inputs.do)
- Aggregate helper: [explorations/python-grc/counterfactuals.py](file:///C:/git/ckt/.claude/worktrees/lca-inversion/explorations/python-grc/counterfactuals.py)
- V2 driver: [explorations/2026-05-18_e1_v2_check.py](file:///C:/git/ckt/.claude/worktrees/lca-inversion/explorations/2026-05-18_e1_v2_check.py)
- CSV inputs: [RP7/output/counterfactual_inputs/](file:///C:/git/ckt/.claude/worktrees/lca-inversion/RP7/output/counterfactual_inputs/)

## Re-run against canonical data and sters (2026-05-20)

The exporter was re-run against the canonical sters in `RP7/output/` (copied from `grc-pipeline-refactor/RP7/output/` on 2026-05-18) with `RP7/data` now junctioned to the canonical `grc-pipeline-refactor/RP7/data`.
The refreshed CSVs in `RP7/output/counterfactual_inputs/` are bit-identical to the snapshot at `RP7/output/counterfactual_inputs_rp5/`, and the V2 driver reproduces the numbers below to four decimals: IDN $W_{\text{obs}} - W_{\text{zero}} = +0.0463$ ($+4.74\%$), gap $= +0.0617$ ($+6.36\%$); TZA $W_{\text{obs}} - W_{\text{zero}} = -0.0964$ ($-9.19\%$), gap $= +0.3251$ ($+38.42\%$).
The data and ster swap is a no-op for the E1 point-estimate aggregate.
What it materially changes is the inversion-CI machinery for V3, where the canonical sters carry the inversion scalars used to construct the joint $(\phi, \beta)$ grid.

## What V2 does

V2 evaluates the E1 misallocation decomposition at the point estimate, using the inputs the production code will consume.
This is a code-consistency check, not a magnitude validation against an external benchmark.
The aggregate formula:

$$W_{\text{obs}} - W_{\text{zero}} = \sum_{\underline{d}} \pi_{\underline{d}} \cdot \Delta_{\underline{d}} \cdot \bar{D}_{\underline{d}}, \qquad W_{\text{opt}} - W_{\text{obs}} = \sum_{\underline{d}} \pi_{\underline{d}} \left[\max(0, \Delta_{\underline{d}}) - \Delta_{\underline{d}} \cdot \bar{D}_{\underline{d}}\right].$$

Trajectory categories used:

- $d_N$ (trajectory $= 1$): $\Delta = \hat\Delta_{d_N}$ from inversion.
- Balanced switchers (trajectory $\in 2 \ldots K-1$): $\Delta_{\underline{d}}$ from the unrestricted GRC `_d` ster.
- $d_T$ (trajectory $= K$): $\Delta = \hat\Delta_{d_T}$ from inversion.
- Lumped unbalanced switchers (trajectory missing, `unbalanced == 1`): $\Delta = $ `xb:unbalanced_choice` from the parent ster.

## Numbers

### IDN col 5

| | log pts | percent (geo-mean) |
|---|---:|---:|
| $W_{\text{obs}} - W_{\text{zero}}$ | $+0.046$ | $+4.7\%$ |
| $W_{\text{opt}} - W_{\text{obs}}$ (Option 1 floor) | $+0.062$ | $+6.4\%$ |

Trajectory shares: $\pi_{d_N} = 0.039$, $\pi_{\text{lumped switcher}} = 0.890$, $\pi_{d_T} = 0.027$, balanced switchers $\approx 0.044$ collectively.

### TZA col 5

| | log pts | percent (geo-mean) |
|---|---:|---:|
| $W_{\text{obs}} - W_{\text{zero}}$ | $-0.096$ | $-9.2\%$ |
| $W_{\text{opt}} - W_{\text{obs}}$ (Option 1 floor) | $+0.325$ | $+38.4\%$ |

Trajectory shares: $\pi_{d_N} = 0.410$, $\pi_{\text{lumped switcher}} = 0.288$, $\pi_{d_T} = 0.199$, balanced switchers $\approx 0.103$.

## Comparison to the 2026-05-13 back-of-envelope

The memo's BoE gave: $W_{\text{obs}} - W_{\text{zero}} \approx -0.04$ (IDN), $\approx -0.09$ (TZA); $W_{\text{opt}} - W_{\text{obs}} \approx 3.7\%$ (IDN), $\approx 22\%$ (TZA).

**TZA**: $W_{\text{obs}} - W_{\text{zero}}$ matches the BoE direction and magnitude ($-0.096$ production vs $-0.09$ memo).
The misallocation gap is larger than the BoE suggested ($+38.4\%$ vs $\approx 22\%$): the lumped switcher cell contributes $+0.069$ log pts and $d_N$ contributes $+0.111$ log pts, neither of which the BoE accounted for in detail.

**IDN**: the production number for $W_{\text{obs}} - W_{\text{zero}}$ flips sign relative to the BoE ($+0.046$ vs $-0.044$).
The flip is mechanical: in IDN's unbalanced spec, 89% of individuals sit in the lumped switcher cell with $\bar D \approx 0.48$ and $\Delta = $ `unb_choice` $= 0.116$, contributing $+0.050$ log pts that the BoE skipped.
The misallocation gap is also larger ($+6.4\%$ vs $\approx 3.7\%$), again driven by the lumped piece.

**V2 verdict: PASS on code consistency.**
The code reproduces the aggregate cleanly on its own inputs.
The BoE numbers from the memo were sketched with only $d_N$ and $d_T$ in mind and skipped the lumped switcher cell, which is large especially in IDN.

## Observations worth carrying forward

1. **The lumped-unbalanced cell is huge in IDN.**
   89% of IDN's unbalanced-spec individuals sit in this cell with non-trivial $\bar D \approx 0.48$ and $\Delta \approx +0.12$.
   For E1 to be defensible in IDN, the paper-side text needs to be transparent about how the lumped cell enters and what its $\Delta$ means.
   The current treatment uses `xb:unbalanced_choice` from the parent ster, which is the GMM's parameterization, but this deserves a one-sentence flag in the paper.

2. **TZA's $d_T$ piece dominates $W_{\text{obs}} - W_{\text{zero}}$ at $-0.139$ log pts.**
   Always-urban workers in TZA have $\hat\Delta_{d_T} = -0.70$ at the point estimate.
   Reading: observed migration has *negative* current welfare from the always-urban cell, consistent with the memo.

3. **TZA's $d_N$ piece dominates the misallocation gap at $+0.111$ log pts.**
   $\pi_{d_N} = 0.41$ and $\hat\Delta_{d_N} = +0.27$ deliver the bulk.
   This is the "non-migrants would gain from migrating" reading.
   The interpolation status (A6: TZA at 8% of switcher hull) means this magnitude rests on LCA holding near the lower edge of the support where it was fit.

4. **The lumped cell needs its own row in T1.**
   Currently the plan's T1 decomposition is $d_N$ / switchers / $d_T$.
   Under the unbalanced headline, the lumped-switcher cell deserves a fourth row.

## Next steps

V2 passed (code consistency) and has been re-verified against canonical data and sters.
The natural next steps in the plan's sequencing:

1. **V3 IDN empty-CI diagnostic**: the joint $(\phi, \beta)$ CI from `build_joint_ci_grid` returned zero accepted lattice points for IDN at $\alpha = 0.05$, even though the marginal $\phi$ CI from `5b_inversion.do` is $[-1.23, -0.01]$.
   Evaluate the constrained-$J$ Wald at the GMM point estimate ($\phi = -0.5247$, $\beta = +0.0672$) and compare to $\chi^2_{27, 0.95} = 40.11$.
   A small Wald confirms the construction is right and the empty CI is honest at the high $K$ dof; a large Wald points to a moment-formula or Jacobian bug.
2. **TZA Möbius-pole fallback** (plan's P3): when the joint CI crosses $\phi = -1$, restrict the aggregate to $d_N$ plus switchers (drop $d_T$).
   TZA's V3 smoke showed 19 accepted points all in $\phi \in [-1.225, -1.050]$, with the aggregate inflated to $+285\%$ from the singularity.
3. **CHN hukou-split inversion CIs**: `5b_inversion.do` is pooled-only.
   The four hukou cells (rf, uf, ro, uo) need an adapted attach pipeline before E2.
4. **Option 3 envelope**: implement the within-trajectory dispersion integration (Py-mod 3) and run V4.
5. **E2 hukou** (requires CHN hukou-split sters with inversion CIs).
