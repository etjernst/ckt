# Verify-snippet report: RP7/README.md

Snippet: the replication README, [RP7/README.md](file:///C:/git/ckt/RP7/README.md), checked at commit `fbc8e2b` (with the 44-to-31 runtime correction already applied).
Sources: the pipeline scripts under `RP7/scripts/`, `0_programs.do`, `0_path_config.do`, `0_setup.do`, `scripts/utilities/`, `explorations/python-grc/lca_inversion.py`, `paper/CKT.bib`.
Decomposition: [2026-07-21_2310_readme_decomposition.md](file:///C:/git/ckt/quality_reports/verify-snippet/2026-07-21_2310_readme_decomposition.md).
Checks: 12 fresh-context verifier groups (V1-V12) plus one alignment critic (V13), every finding line-anchored to code.

Verdict: FIX.
Ten findings, all of them README wording or internal consistency; none blocks a default replication run, and no code defect was found on the pipeline path.
The description sections for `0_CHN_hukou_restrictions`, `1_processData`, `4_GrRC`, `5_GrRC_NonAg`, `7_GrRC_hukou`, `8_learning`, `9_GRC_extras`, `10_make_tables` (bar one count), `11_make_figures`, `11b`, `17_verdier_robust`, `17b_cluster_summary`, the utilities bullets, the no-randomness claim, the Python package list, the Stata package list, the output-location map, the script ordering, and all four references verified clean against the code.

## Findings (each with the fix it calls for)

- F1, line 166 (V2, PARTIAL). "This script relies only on built-in Stata commands, no shared programs from `0_programs.do`" overclaims: `1b` conditionally calls `copyOverleaf` at its tail. Fix: scope the claim to the diagnostic computation and name the optional copy step.
- F2, line 172 (V3, NOT-VERIFIED). "Each table stacks all three countries in a three-panel layout" is false for the Indonesia-only nonag table (`panels(1), countries(IDN)`). Fix: attribute the three-panel layout to the two urban tables only.
- F3, line 208 (V5, PARTIAL). "Retained observation count" misdescribes `_n_kept`, which counts switcher trajectories surviving the sparsity threshold (`len(kept)`), not rows. Fix: rename the quantity in prose.
- F4, line 250 (V5, NOT-VERIFIED). 5c runs "across all five covariate columns": the `covs_0` iteration always hits the no-parent-ster skip because `7_GrRC_hukou.do` comments c0 out, so four columns receive CIs. Fix: say four estimated columns, with the fifth loop token skipping.
- F5, line 291 (V9, NOT-VERIFIED). "Forty-four files in total" for the extras tables; the enumerated `extras_tex_table` calls compute to 31, and the README's own descriptive clause implies 31. The 44 is the stale income-era count. Fix: thirty-one.
- F6, line 69 (V12, NOT-VERIFIED). "Every do-file declares `version 17`" is false: only `0_master.do` and the utilities carry the declaration; the 21 core pipeline scripts have none. The run is still version-17-governed because everything executes via `include` under the master's declaration. Fix: state the master-level declaration accurately; adding per-script declarations is a separate post-run code decision.
- F7, lines 385-387 (V12 + V13 Finding 1, MAJOR). "Two globals gate optional steps" undercounts: `$runDashboard` is a third, gating a dev-only `shell python` call whose target lives outside the shipped package tree. Fix: three globals, with a bullet marking `$runDashboard` dev-only and to be left at 0.
- F8, lines 364-368 (V13 Finding 2, MAJOR). "Auxiliary programs, called from within the numbered scripts above" contradicts line 109 and the code: `0_path_config`/`0_setup`/`0_programs` are included once by `0_master.do` before the numbered sequence. Fix: reword to the include-once structure.
- F9, line 72 (V13 Finding 4, MINOR). `0_setup.do` is the first dependency-install step, not literally the first step (`0_path_config.do` precedes it). Fix: one word.
- F10, line 383 (V13 Finding 5, MINOR). "Set the `$dir` global" understates the task: the per-user block is username-conditional, so an outside replicator must add a block for their own `c(username)`, not edit a single line. Fix: say so. Whether to collapse the per-user blocks into one generic block before the `ReplicationPackage7` handoff is an author decision, parked.

## Caveats recorded, no action

- hukou==1-is-rural is verified only by the script's internal naming consistency; no CFPS codebook lives in the repo (V1).
- `heterogeneity_plots` saves intermediate `.gph` panels under `.pdf`-suffixed names before the real exports; cosmetic (V10).
- `n_skipped` in the 11b support test counts singletons below the identified edge, not all singletons; consistent with the README's framing (V10).

## Parked for post-run (code, not README; the running pipeline is not edited mid-run)

- `0_master.do:131` comment says "44 stems" for the extras include; true count 31 (V13 Finding 3, resolved by V8/V9's counts).
- `9_GRC_extras.do` header lines still say "44 stems (220 sters)"; income-era counts.
- Per-script `version 17` declarations for the 21 core scripts: author decision.
- `run_master_resume.do`'s stale worktree `$dir` line was already fixed during this pass (the one safe code edit, since the running batch never reads that file).
- Per-user `$dir` block collapse and the `$runDashboard` external-path dependency: pre-handoff decisions.
