# Writing critic report: cluster-specific trajectory intercepts subsection

Target: [sections/sec_robustness.tex](file:///C:/Users/maand/Monash%20Uni%20Enterprise%20Dropbox/Emilia%20Tjernstrom/Apps/Overleaf/ReturnsToMigration-clean/sections/sec_robustness.tex)
Scope: lines 38--100 (the `\subsection{Allowing ... trajectory intercepts}` block).
Run: 2026-04-30. Generated by `writing-critic` agent (returned inline; saved here for the audit trail).

---

## CRITICAL issues

### C0. Heading mismatch (location vs cluster)

Line 38: `\subsection{Allowing location-specific trajectory intercepts}`---body uniformly speaks of clusters (village $v_i$). Fix: rename to `\subsection{Allowing cluster-specific trajectory intercepts}`.

### C1. Bullet/dot subscripts in the cluster-residualized instrument

Lines 47--50. The display equation introduces $D_{\cdot\cdot}$, $\underline d_{\cdot}$, and $\overline{\cdots}_{v_i,\,\underline d}$ without telling the reader the dots stand for indices being averaged over. Either (a) replace the bar notation with an explicit double sum:
```
\tilde D_{it}^{\,\underline d} \;\equiv\; D_{it}\,\mathbbm 1\{\underline d_i=\underline d\} \;-\; \frac{1}{|\{(j,s): v_j = v_i,\, \underline d_j = \underline d\}|}\sum_{\substack{j:\,v_j=v_i\\ \underline d_j=\underline d}}\sum_{s} D_{js}\,\mathbbm 1\{\underline d_j=\underline d\}.
```
or (b) keep the compact form and add a one-sentence convention gloss after the display:
```
We use the convention that a dot in a subscript denotes the index being averaged over: $D_{\cdot\cdot}$ is the average of $D_{js}$ over $(j,s)$, and $\underline d_{\cdot}$ is the corresponding worker-level trajectory label. The subscripts $v_i, \underline d$ on the overbar restrict that average to workers in cluster $v_i$ on trajectory $\underline d$.
```

### C2. $v_i$ used before defined

The instrument equation uses $v_i$ (line 48) and so does the surrounding prose (lines 50, 52), but the formal definition is at line 56. Move the `Let $v_i \in \{1,\ldots,V\}$ index the cluster (village, in our data) of worker $i$.` sentence to immediately precede line 41.

---

## MAJOR issues

### M1. "to probe this margin" (AI tell)

Line 44: replace with `To check whether between-cluster heterogeneity is driving $\hat\phi$, we re-estimate the same moment system with one intercept per (trajectory, cluster) cell, holding the LCA slope $\phi$ common across cells.`

### M2. "Read together, ..., to a first approximation" (AI tell + filler)

Line 83: rewrite as `Taken across the three countries, these comparisons show that the trajectory-pooling step in our baseline is not absorbing meaningful between-cluster variation in mean rural consumption: the cross-trajectory $(\mu_{\underline d}, \Delta_{\underline d})$ scatter that identifies $\hat\phi$ in the baseline is close to the scatter the within-cluster moments deliver.`

### M3. "loads on between-cluster differences" (factor-analysis vocab)

Line 51: `removes any variation in $D$ that loads on between-cluster differences within a trajectory`---swap to `removes any between-cluster variation in $D$ within a trajectory`.

### M4. "reads off the cross-trajectory ... scatter" (loose verb)

Line 42: replace with `The slope $\phi$, identified in the restricted GRC by the regression of $\Delta_{\underline d}$ on $\mu_{\underline d}$ across switcher trajectories, could then be biased by between-cluster shifts in mean consumption rather than by genuine comparative advantage.`

### M5. $c(v_i)$ and $\bar c(\underline d)$ defined imprecisely

Lines 69--72: replace the post-equation gloss with
```
where $c(v_i) \equiv E[Y_{it}^R \mid v_i] - E[Y_{it}^R]$ is the cluster fixed effect in mean rural log consumption (orthogonal by construction to the unconditional rural mean) and $\bar c(\underline d) \equiv E[c(v_i)\mid \underline d_i=\underline d]$ is its trajectory-conditional mean.
The bias vanishes when trajectory assignment is mean-independent of $c(v_i)$, which is exactly the content of Assumption~\ref{ass:cluster-pooling}.
```

### M6. (A1)--(A5) referenced without anchor

Line 65: `(A1)--(A5)` has no `\ref`. Replace with `assumptions (A1)--(A5) of Section~\ref{subsec:empirical-model}` and fix subject-verb agreement (`the latter restrict`).

### M7. "matching the implementation in Verdier"

Line 76: prefer `following \citet{verdierAverageTreatmentEffects2020a}.`

### M8. "the small movement there is uninformative"---effect size missing

Line 80: cite the China column-5 standard error directly. Either include the SE or hedge as `the China column-5 estimate has a standard error roughly the same size as the point estimate under both specifications`.

### M9. Significance and effect size separated across two sentences

Lines 79 and 81: merge so significance lands with the magnitude:
`With the full set of covariates (column 5), $\hat\phi^{\mathrm{rob}}$ moves from $-0.525$ to $-0.334$ in Indonesia (significant at the 5\% level), from $-0.719$ to $-0.690$ in Tanzania (significant at the 1\% level), and from $-0.205$ to $-0.130$ in China.`

### M10. Convergence-failure caveat buried in footnote

Line 76 footnote: promote the IDN+CHN twostep convergence failure into the body.

### M11. "validates pooling" (overclaim)

Line 100: rewrite as `Proposition~\ref{prop:pooling} gave sufficient conditions for pooling across the balanced and unbalanced strata; Proposition~\ref{prop:robust-equivalence} plays the analogous role for pooling across clusters within trajectory.`

---

## MINOR

### N1. Two em dashes in one sentence (line 41)

Borderline acceptable; could swap one pair for parens if dialing down.

### N2. "trajectory-cell framework" jargon (lines 91--92)

Either gloss or replace with `our trajectory-cell setup---one moment per (trajectory, covariate) cell`. Then line 92 becomes `without leaving the GRC moment system.`

### N3. "the LCA slope" / "$\mathcal D_S$" used unanchored

Insert a single inline gloss when LCA first appears (line 44), then no further glosses needed.

---

## Voice notes

Strong opening (line 40). Good quantification in the empirical paragraph. AI-tell hits: "to probe this margin" (M1), "Read together / to a first approximation" (M2). No `interestingly`, `it is worth noting`, `arguably`, `novel`, etc. American English clean. Em dashes flush. One sentence per source line. No bold labels, no `\paragraph{}`, no `\emph{}`.

---

## Score and readiness

| Category | Score |
|---|---|
| Hard rules (notation, headings, em dashes, AmEng, sentence-per-line) | 42 / 60 |
| Preferred defaults (AI-tell, imprecise verbs, factor-analysis vocab, evidence-to-claim) | 12 / 20 |
| Voice consistency | 15 / 20 |
| **Total** | **69 / 100** |

Not ready. C0, C1, C2 block submission regardless of score. Once those plus M1--M5 are fixed, the subsection should clear 90.
